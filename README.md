# 个人博客系统

一个基于 React + TypeScript + Supabase + Vercel 构建的现代化个人博客系统。

## 功能特性

### 前端功能
- 📝 文章发布和管理
- 🏷️ 分类管理
- 💬 评论系统（支持审核）
- 🔍 文章搜索
- 📱 响应式设计
- 🎨 Markdown 编辑器（支持实时预览）
- 🌓 语法高亮显示

### 管理后台
- 🔐 管理员登录认证
- 📝 文章管理（创建、编辑、删除、发布/草稿）
- 🏷️ 分类管理
- 💬 评论审核（待审核、已批准、已拒绝）
- 📊 数据统计仪表板

### 技术栈
- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **后端服务**: Supabase（数据库、认证、存储）
- **Markdown 渲染**: react-markdown + remark-gfm
- **语法高亮**: Prism.js
- **部署**: Vercel

## 快速开始

### 环境要求
- Node.js 18+
- npm 或 pnpm

### 安装依赖
```bash
npm install
```

### 环境变量配置
创建 `.env` 文件并配置以下内容：
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 开发环境启动
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 代码检查
```bash
npm run check
```

## 项目结构

```
src/
├── components/          # 可复用组件
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── PostCard.tsx
│   ├── Comments.tsx
│   └── MarkdownRenderer.tsx
├── pages/              # 页面组件
│   ├── Home.tsx
│   ├── PostDetail.tsx
│   ├── Category.tsx
│   ├── About.tsx
│   └── admin/          # 管理后台页面
│       ├── Login.tsx
│       ├── Dashboard.tsx
│       ├── Posts.tsx
│       ├── PostEditor.tsx
│       ├── Categories.tsx
│       └── Comments.tsx
├── lib/                # 工具函数和 API
│   ├── supabase.ts
│   ├── api.ts
│   ├── adminApi.ts
│   └── utils.ts
├── types/              # TypeScript 类型定义
└── App.tsx
```

## 数据库结构

### 文章表 (posts)
- id (UUID, 主键)
- title (文本, 标题)
- content (文本, 内容)
- excerpt (文本, 摘要)
- slug (文本, URL 别名)
- published (布尔值, 发布状态)
- read_time (整数, 阅读时间)
- category_id (UUID, 分类 ID)
- created_at (时间戳)
- updated_at (时间戳)

### 分类表 (categories)
- id (UUID, 主键)
- name (文本, 分类名称)
- slug (文本, URL 别名)
- description (文本, 描述)
- post_count (整数, 文章数量)
- created_at (时间戳)

### 评论表 (comments)
- id (UUID, 主键)
- post_id (UUID, 文章 ID)
- author_name (文本, 评论者姓名)
- author_email (文本, 评论者邮箱)
- content (文本, 评论内容)
- status (文本, 状态: pending/approved/rejected)
- reply (文本, 管理员回复)
- approved (布尔值, 是否已批准)
- created_at (时间戳)

## 部署说明

### Vercel 部署
1. 将代码推送到 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量
4. 部署应用

### 环境变量配置（Vercel）
在 Vercel 项目设置中添加以下环境变量：
- `VITE_SUPABASE_URL`: Supabase 项目 URL
- `VITE_SUPABASE_ANON_KEY`: Supabase 匿名密钥

## 安全说明

- 使用 Supabase RLS（行级安全）策略保护数据
- 管理员功能需要身份验证
- 评论需要审核才能显示
- 使用环境变量保护敏感信息

## 开发说明

### 添加新功能
1. 更新数据库结构（创建迁移文件）
2. 更新 TypeScript 类型定义
3. 实现 API 函数
4. 创建或更新组件
5. 测试功能

### 代码规范
- 使用 TypeScript 进行类型检查
- 遵循 React 最佳实践
- 使用 Tailwind CSS 进行样式设计
- 保持组件小而专注

## 许可证

MIT License